/**
 * Settings of load window or body
 */
const TIMEOUT_CONTENT_LOADED = 1000

/**
 * Settings global informations
 */
const DEVELOPER_MODE = true

const API_URI = "http://localhost:8080"

export {
    TIMEOUT_CONTENT_LOADED,
    DEVELOPER_MODE,
    API_URI
}